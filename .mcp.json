{
  "mcpServers": {
    "usage-pulse": {
      "command": "uv",
      "args": [
        "run",
        "--project",
        "${CLAUDE_PLUGIN_ROOT}",
        "python",
        "-m",
        "usage_pulse.mcp_server"
      ],
      "cwd": "${CLAUDE_PLUGIN_ROOT}"
    }
  }
}
